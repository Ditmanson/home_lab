apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: techtdebian
  namespace: hugo
spec:
  hosts:
    - "tech.tdebian.com"
  gateways:
    - nginx-gateway
  http:
    - match:
        - uri:
            prefix: "/"
          headers:
            host:
              exact: "192.168.1.246"
        - uri:
            prefix: "/"
          headers:
            host:
              exact: "tech.tdebian.com"
      route:
        - destination:
            host: nginx-svc-tech.hugo.svc.cluster.local
